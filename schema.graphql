type User {
  name: String!
  email: String!
  userType: String!
  calorieGoal: Int!
  weight: Int
}

type Food {
  name: String!
  caloriesPerServ: Int!
  category: Category!
}

type FoodEntry {
  date: DateTime!
  food: Food!
  user: User!
  servingQty: Int!
  mealCategory: MealCategory!
}
